name: Trigger Test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/trigger.yml
      - hoge.txt
  pull_request:
    paths:
      - .github/workflows/trigger.yml
      - hoge.txt

jobs:
  trigger_test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: echo 'triggered!'
