name: Trigger Test

on:
  push:
    branches:
      - main
    paths: &paths
      - hoge.txt
  pull_request:
    paths: *paths

jobs:
  trigger_test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: echo 'triggered!'
