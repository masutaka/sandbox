on:
  # schedule:
  #   - cron: "0 0 * * mon"
  workflow_dispatch:

jobs:
  create_discussion:
    uses: route06/actions/.github/workflows/create_discussion.yml@baba-add-workflow
    with:
      target_day: wednesday
      title: 大事な話
      discussion_category_slug: meeting-notes
      description_template_path: .github/workflows/templates/important_discussion.yml
